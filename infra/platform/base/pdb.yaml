apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: api
  namespace: vibeyeeter-system
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: api
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web
  namespace: vibeyeeter-system
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: web
